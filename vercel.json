{
  "buildCommand": "cd returnshield-frontend && npm install && npm run build",
  "outputDirectory": "returnshield-frontend/dist",
  "rewrites": [
    { "source": "/((?!api|.*\\.).*)", "destination": "/index.html" }
  ]
}
